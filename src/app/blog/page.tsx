import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: {
    absolute: "Blog",
  },
};

const Page = () => {
  return <div>thi is page route</div>;
};

export default Page;
